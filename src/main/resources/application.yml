#默认端口配置
server:
  port: 9090

#数据库连接配置
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/manger?useUnicode=true&amp;characterEncoding=UTF-8?useSSL=false
    username: root
    password: root
#mybatis的配置
#mybatis:
  # 映射文件所在路径
  #  mapper-locations: classpath:mapping/*.xml
  # pojo类所在包路径
#  type-aliases-package: cn.liwei08.entity
